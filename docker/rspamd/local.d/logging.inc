# =============================================================================
# Rspamd Logging — reduce noise from disabled RBLs
# =============================================================================

level = "notice";
log_type = "console";
